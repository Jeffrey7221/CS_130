# Provided config file - Expect Success

foo "bar";

server {
  listen 8080;

  static handler {
    location /static/;
    root /tests/static_data;
  }

  static handler {
    location /static_2/;
    root /tests/example_configs;
  }

  echo handler {
    location /echo/;
  }
}