# Format of config file we use for our webserver

server {
  port 8080;
  server_name foo.com;
  location "/" StaticHandler {
    root /data;
  }

  location "/static" StaticHandler {
    root /data;
  }

  location "/status" StatusRequestHandler {
  }

  location "/echo" EchoHandler {
  }
}